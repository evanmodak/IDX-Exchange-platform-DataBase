# Google Maps Embed API key (used by PropertyMap component)
REACT_APP_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Workaround for a known CRA/webpack-dev-server issue that occurs when
# a "proxy" field is set in package.json
DANGEROUSLY_DISABLE_HOST_CHECK=true
